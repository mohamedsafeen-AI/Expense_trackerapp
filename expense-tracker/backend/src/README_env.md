# Backend environment variables

Set these for local development or your hosting provider.

- `SUPABASE_URL`
- `SUPABASE_SERVICE_ROLE_KEY` (service role key)

The app has **no authentication**; the backend uses the service role key.

